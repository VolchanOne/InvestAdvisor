@using InvestAdvisor.Common.Enums
@using InvestAdvisor.Model
@model InvestAdvisor.Model.PaymentSystemModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var currencies = (List<CurrencyModel>) ViewBag.Currencies;
}

<div class="container-fluid">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Редактирование</h4>
            <div class="row">
                <div class="col-sm-12">
                    @using (Html.BeginForm("Edit", "PaymentSystems", FormMethod.Post, new { @class = "form-material", enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.PaymentSystemId)

                        <div class="form-group">
                            @Html.LabelFor(m => m.Name)
                            @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(m => m.ShortName)
                            @Html.TextBoxFor(m => m.ShortName, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(m => m.Url)
                            @Html.TextBoxFor(m => m.Url, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn waves-effect waves-light btn-outline-info">Сохранить</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Обзор</h4>
            <div class="row">
                <div class="col-sm-12">
                    @using (Html.BeginForm("EditReview", "PaymentSystems", FormMethod.Post, new { @class = "form-material", enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.PaymentSystemId)
                        @Html.HiddenFor(model => model.Review.ProjectReviewId)

                        <div class="form-group">
                            @Html.TextAreaFor(m => m.Review.Review, new { @class = "textarea_editor form-control", @rows = 15 })
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn waves-effect waves-light btn-outline-info">Сохранить</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-sm-12">
                    @using (Html.BeginForm("AddImage", "PaymentSystems", FormMethod.Post, new { @class = "form-material", enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.PaymentSystemId)
                        <h4>Добавить изображение</h4>
                        <div class="form-group">
                            <div>
                                <input type="file" class="form-control" name="Image" aria-describedby="fileHelp">
                            </div>
                        </div>
                        <div class="form-group">
                            @Html.DropDownList("ImageType",
                                                     EnumHelper.GetSelectList(typeof(ImageType)),
                                                     "Выберите тип",
                                                     new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn waves-effect waves-light btn-outline-info">Сохранить</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
    @if (Model.Images != null && Model.Images.Count > 0)
    {
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Изображения платежной системы</h4>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Изображение</th>
                                <th>Тип</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @{int i = 1;}
                            @foreach (var image in Model.Images)
                            {
                                <tr>
                                    <td>
                                        @i
                                    </td>
                                    <td>
                                        <img src="data:image;base64,@Convert.ToBase64String(image.Content)" width="150" />
                                    </td>
                                    <td>
                                        @image.ImageType.ToString()
                                    </td>
                                    <td>
                                        <button type="button" class="btn waves-effect waves-light btn-secondary">
                                            @Html.ActionLink("Удалить", "DeleteImage", new { paymentSystemId = Model.PaymentSystemId, imageId = image.ImageId })
                                        </button>
                                    </td>
                                </tr>
                                i++;
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Валюты</h4>
            <div class="row">
                <div class="col-sm-12">
                    @using (Html.BeginForm("EditCurrencies", "PaymentSystems", FormMethod.Post, new { @class = "form-material", enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.PaymentSystemId)
                        <select name="Currencies[]" class="selectpicker" multiple data-style="form-control btn-secondary">
                            @if (currencies != null)
                            {
                                foreach (var currency in currencies)
                                {
                                    <option value="@currency.CurrencyId" @(Model.Currencies.Any(p => p.CurrencyId == currency.CurrencyId) ? "selected" : string.Empty)>@currency.Abbreviation.ToString()</option>
                                }
                            }
                        </select>
                        <div class="form-group">
                            <button type="submit" class="btn waves-effect waves-light btn-outline-info">Сохранить</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <button type="button" class="btn waves-effect waves-light btn-secondary">
        @Html.ActionLink("Назад к списку", "Index")
    </button>

</div>


